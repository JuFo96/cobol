services:
  postgres:
    image: postgres
    container_name: pg_cobol
    restart: always
    environment:
      POSTGRES_USER: admin    
      POSTGRES_PASSWORD: password
      POSTGRES_DB: cobol_db
    volumes:
      - postgres_data:/var/lib/postgresql
    ports:
      - '5433:5433'

volumes:
  postgres_data:
